# ===========================================
# TgNetBucket 环境变量配置
# ===========================================

# ===========================================
# Telegram Bot Configuration
# ===========================================
# 从 @BotFather 获取的 Bot Token
# 必需：用于与 Telegram API 通信
TELEGRAM_BOT_TOKEN=your_bot_token_here

# 用于存储文件的 Telegram 聊天 ID
# 可以是个人聊天ID或群组ID（建议使用私有群组）
# 必需：文件将上传到此聊天中
TELEGRAM_CHAT_ID=your_chat_id_here

# Telegram API 配置
# 可选：自定义 Telegram API 服务器（默认使用官方API）
# TELEGRAM_API_URL=https://api.telegram.org

# 文件上传超时时间（毫秒）
# 可选：默认 30000 (30秒)
# TELEGRAM_UPLOAD_TIMEOUT=30000

# ===========================================
# Redis Configuration
# ===========================================
# Upstash Redis Configuration (可选，用于持久化存储文件列表)
# 从 https://upstash.com/ 获取免费Redis数据库配置
# 如果不配置，将使用内存存储（仅适用于开发环境）
UPSTASH_REDIS_REST_URL=your_upstash_redis_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_here

# Redis 连接配置
# 可选：Redis 连接超时时间（毫秒）
# REDIS_TIMEOUT=5000

# 可选：Redis 重试次数
# REDIS_RETRY_ATTEMPTS=3

# ===========================================
# Admin Authentication Configuration
# ===========================================
# 管理员认证配置，用于保护文件上传和管理功能
# 管理员用户名（建议修改默认值）
ADMIN_USERNAME=admin

# 管理员密码（请使用强密码）
ADMIN_PASSWORD=your_secure_password_here

# JWT 密钥（用于生成认证令牌）
# 可选：如果不设置，将使用默认密钥（不推荐在生产环境使用）
# JWT_SECRET=your_jwt_secret_here

# 认证令牌过期时间（小时）
# 可选：默认 24 小时
# AUTH_TOKEN_EXPIRES_IN=24

# ===========================================
# Application Configuration
# ===========================================
# 应用基础URL（用于生成下载链接等）
# 可选：在 Vercel 等平台部署时会自动检测
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# 应用名称
# 可选：默认 "TgNetBucket"
# NEXT_PUBLIC_APP_NAME=TgNetBucket

# 应用版本
# 可选：默认从 package.json 读取
# NEXT_PUBLIC_APP_VERSION=1.0.0

# ===========================================
# File Upload Configuration
# ===========================================
# 最大文件大小（字节）
# 可选：默认 50MB (50 * 1024 * 1024)
# MAX_FILE_SIZE=52428800

# 最大总存储大小（字节）
# 可选：默认 2GB (2 * 1024 * 1024 * 1024)
# MAX_TOTAL_SIZE=2147483648

# 允许的文件类型（逗号分隔）
# 可选：如果不设置，允许所有类型
# ALLOWED_FILE_TYPES=image/*,video/*,audio/*,application/pdf,text/*

# 文件名最大长度
# 可选：默认 255
# MAX_FILENAME_LENGTH=255

# ===========================================
# Security Configuration
# ===========================================
# CORS 允许的域名（逗号分隔）
# 可选：默认允许所有域名（开发环境）
# CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# 启用 HTTPS 重定向
# 可选：默认 false（开发环境）
# FORCE_HTTPS=true

# 安全头配置
# 可选：启用安全响应头
# ENABLE_SECURITY_HEADERS=true

# ===========================================
# Performance Configuration
# ===========================================
# 启用文件缓存
# 可选：默认 true
# ENABLE_FILE_CACHE=true

# 缓存过期时间（秒）
# 可选：默认 3600 (1小时)
# CACHE_EXPIRES_IN=3600

# 启用 Gzip 压缩
# 可选：默认 true
# ENABLE_GZIP=true

# ===========================================
# Logging Configuration
# ===========================================
# 日志级别 (error, warn, info, debug)
# 可选：默认 info
# LOG_LEVEL=info

# 启用访问日志
# 可选：默认 true
# ENABLE_ACCESS_LOG=true

# 启用错误日志
# 可选：默认 true
# ENABLE_ERROR_LOG=true

# ===========================================
# Development Configuration
# ===========================================
# 开发模式
# 可选：默认根据 NODE_ENV 判断
# NODE_ENV=development

# 启用调试模式
# 可选：默认 false
# DEBUG=false

# 启用热重载
# 可选：默认 true（开发环境）
# ENABLE_HOT_RELOAD=true

# ===========================================
# Analytics Configuration (可选)
# ===========================================
# Google Analytics ID
# 可选：用于网站分析
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# 启用使用统计
# 可选：默认 false
# ENABLE_ANALYTICS=false